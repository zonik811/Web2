export const semanticColors = {
    primary: "var(--primary)",
    secondary: "var(--secondary)",
    background: "var(--background)",
    foreground: "var(--foreground)",
    muted: "var(--muted)",
    mutedForeground: "var(--muted-foreground)",
    accent: "var(--accent)",
    accentForeground: "var(--accent-foreground)",
    destructive: "var(--destructive)",
    destructiveForeground: "var(--destructive-foreground)",
    border: "var(--border)",
    input: "var(--input)",
    ring: "var(--ring)",
};

export const palette = {
    // Primitives if needed for gradients or fixed values
    black: "#000000",
    white: "#ffffff",
    transparent: "transparent",

    // Gradients presets
    gradients: {
        cosmic: "bg-gradient-to-r from-purple-500 via-pink-500 to-orange-500",
        ocean: "bg-gradient-to-r from-blue-500 to-teal-500",
        sunset: "bg-gradient-to-r from-orange-400 to-rose-400"
    }
};
